# Google Gemini API Key
# Get your API key from: https://aistudio.google.com/app/apikey
#
# For development, create a .env file in the root directory and add:
# GEMINI_API_KEY=your_api_key_here
#
# Note: If GEMINI_API_KEY is not set, users will be prompted to enter their API key
# in the app. The key will be stored in sessionStorage (clears when tab closes).

GEMINI_API_KEY=your_api_key_here
